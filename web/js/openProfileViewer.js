
function viewProfile()
{
    $("#profileInfoTable").on('click', 'td', function ()
    {
                if ($(this).children().length === 0)
                {
                    if ($(this).parent().attr('id') === 'profileId')
                    {
                        
                    }
                }
    });
};

